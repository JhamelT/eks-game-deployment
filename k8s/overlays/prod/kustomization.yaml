apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

patches:
# Enable HTTPS with ACM certificate and HTTP→HTTPS redirect
- path: ingress-patch.yaml
  target:
    kind: Ingress
    name: ingress-2048
